name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
    types: [opened, synchronize, reopened]



jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."
      - run: echo "🐧 This job is now running on a ${{ runner.os }} server hosted by GitHub!"
      - run: echo "🔎 The name of your branch is ${{ github.ref }} and your repository is ${{ github.repository }}."

      - name: Check out repository code
        uses: actions/checkout@v5
      - run: echo "💡 The ${{ github.repository }} repository has been cloned to the runner."
      - run: echo "🖥️ The workflow is now ready to test your code on the runner."

      - name: List files in the repository
        run: |
          ls ${{ github.workspace }}
      - run: echo "🍏 This job's status is ${{ job.status }}."

  build-and-analyze:
    runs-on: ubuntu-latest
    services:
      mysql-server:
        image: mysql:8.0
        env:
          MYSQL_ROOT_PASSWORD: ${{ secrets.DB_PASSWORD }}
          MYSQL_DATABASE: ${{ secrets.DB_NAME }}
        ports:
          - 3306:3306
        options: >-
          --health-cmd="mysqladmin ping --silent"
          --health-interval=10s
          --health-timeout=5s
          --health-retries=3
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up JDK21
        uses: actions/setup-java@v4
        with:
          java-version: 21
          distribution: 'temurin'

      - name: Cache SonarQube packages
        uses: actions/cache@v4
        with:
          path: ~/.sonar/cache
          key: ${{ runner.os }}-sonar
          restore-keys: ${{ runner.os }}-sonar

      #- name: Run unit tests
       # run: mvn test -DskipITs=true

      #- name: Run integration tests
       # run: mvn verify -DskipTests=false

      - name: Cache Maven packages
        uses: actions/cache@v4
        with:
          path: ~/.m2
          key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}
          restore-keys: ${{ runner.os }}-m2

      - name: Build and analyze
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        run: mvn -B verify org.sonarsource.scanner.maven:sonar-maven-plugin:sonar -Dsonar.projectKey=vincefgt_apiWeb

      - name: Upload a Build Artifact
        uses: actions/upload-artifact@v4
        with: # Artifact name
          name: package-jar  # optional, default is artifact
          path: target/*.jar # A file, directory or wildcard pattern that describes what to upload
          #Available Options
          if-no-files-found: warn # The desired behavior if no files are found using the provided path. # optional, default is warn
          retention-days: 30 # Duration after which artifact will expire in days. 0 means using default retention.
          compression-level: 6# optional
          # The level of compression for Zlib to be applied to the artifact archive. The value can range from 0 to 9: - 0: No compression
          # - 1: Best speed - 6: Default compression (same as GNU Gzip) - 9: Best compression Higher levels will result in better compression, but will take longer to complete.
          # For large files that are not easily compressed, a value of 0 is recommended for significantly faster uploads.
          overwrite: false # optional, default is false
          # If true, hidden files will be included in the artifact. If false, hidden files will be excluded from the artifact.
          include-hidden-files: false # optional, default is false

        # Cache MySQL data
      - name: Cache MySQL data
        uses: actions/cache@v4
        with:
          path: |
            /var/lib/mysql
            ~/.mysql
          key: ${{ runner.os }}-mysql-${{ hashFiles('**/schema.sql', '**/data.sql') }}
          restore-keys: |
            ${{ runner.os }}-mysql-

      # Attendre que MySQL soit prêt
      - name: Wait for MySQL
        run: |
          until mysqladmin ping -h"127.0.0.1" -P"3306" --silent; do
            echo "Waiting for MySQL..."
            sleep 2
          done
          echo "✅ MySQL is ready!"

      # Charger le schéma et les données
      - name: Load database schema and data
        run: |
          mysql -h 127.0.0.1 -P 3306 -u root -proot test_db < src/main/resources/schema.sql
          mysql -h 127.0.0.1 -P 3306 -u root -proot test_db < src/main/resources/data.sql
          echo "✅ D  # Cache MySQL data
      - name: Cache MySQL data
        uses: actions/cache@v4
        with:
          path: |
            /var/lib/mysql
            ~/.mysql
          key: ${{ runner.os }}-mysql-${{ hashFiles('**/schema.sql', '**/data.sql') }}
          restore-keys: |
            ${{ runner.os }}-mysql-

      # Attendre que MySQL soit prêt
      - name: Wait for MySQL
        run: |
          until mysqladmin ping -h"127.0.0.1" -P"3306" --silent; do
            echo "Waiting for MySQL..."
            sleep 2
          done
          echo "✅ MySQL is ready!"

      # Charger le schéma et les données
      - name: Load database schema and data
        run: |
          mysql -h 127.0.0.1 -P 3306 -u root -proot test_db < src/main/resources/schema.sql
          mysql -h 127.0.0.1 -P 3306 -u root -proot test_db < src/main/resources/data.sql
          echo "✅ D  # Cache MySQL data
      - name: Cache MySQL data
        uses: actions/cache@v4
        with:
          path: |
            /var/lib/mysql
            ~/.mysql
          key: ${{ runner.os }}-mysql-${{ hashFiles('**/schema.sql', '**/data.sql') }}
          restore-keys: |
            ${{ runner.os }}-mysql-

      # Attendre que MySQL soit prêt
      - name: Wait for MySQL
        run: |
          until mysqladmin ping -h"127.0.0.1" -P"3306" --silent; do
            echo "Waiting for MySQL..."
            sleep 2
          done
          echo "✅ MySQL is ready!"

      # Charger le schéma et les données
      - name: Load database schema and data
        run: |
          mysql -h 127.0.0.1 -P 3306 -u root -proot test_db < src/main/resources/schema.sql
          mysql -h 127.0.0.1 -P 3306 -u root -proot test_db < src/main/resources/data.sql
          echo "✅ Database loaded!"

      - run: echo "job's status is ${{job.status}}."
